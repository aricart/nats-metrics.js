{
  "name": "nats-metrics",
  "version": "1.0.8",
  "description": "Metrics tooling  for node-nats and derivatives",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p ./bin && echo '#!/usr/bin/env node\n' > ./bin/nats-metrics && cat ./index.js >> ./bin/nats-metrics"
  },
  "bin": {
    "nats-metrics": "bin/nats-metrics"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com//aricart/node-nats-metrics"
  },
  "author": "Alberto Ricart",
  "license": "Apache-2.0",
  "dependencies": {
    "console.table": "^0.10.0"
  }
}
